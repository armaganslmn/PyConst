# PyConst
Write only key-value store ADT implementation. Primarily for enabling constants in Python.

For Python versions: Python2.7 and later.

Main purpose is to provide constant variable semantics present in static languages.
